mixin header(spec)
  - spec = spec || {}
  - spec.__class = spec.__class || ''

  .menu(class=spec.__class)&attributes(attributes)
    nav
      ul
        li find a store
        li wine club
        li winemakers
      .logo
        img(src="/images/placeholder100x100.png", alt="")
      ul
        li shop
        li login
        li my cart

