mixin cookbook(spec)
  - spec = spec || {}
  - spec.__class = spec.__class || ''

  .cookbook(class=spec.__class)&attributes(attributes)
    h2 Food & Wine from Jo's cookbook
    p view all recipes

    ul.list-of-recipes
      each food in site.data.cookbook
        article
          .img-holder(style="background: url('#{food.image}') no-repeat center;")
            h2 #{food.title}
          p.large #{food.subtitle}
          p.descr #{food.description}
          a(href="#") view #{food.subtitle}


